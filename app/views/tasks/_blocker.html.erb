<ul class="menu-option">
	 <li>
		<%= label_tag "blocker_#{blocker.id}", blocker.detail %>
	 </li>
	 <li>
			<%= link_to blocker, method: :delete, data: {confirm: "Are you sure?"}, remote: true do %>
				<i class="material-icons">delete</i>
			<% end %>
		</li>
</ul>
